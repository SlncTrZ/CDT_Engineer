# CDT_Engineer provider runtime + validation dependencies
# NOTE: fastmcp[client] extra is required — tests/test_mcp_provider.py imports
# fastmcp.client, which fails without client support (verified 2026-09-17:
# fastmcp 3.4.7 client works with mcp 1.30.0, breaks with mcp 2.2.0).
fastmcp[client]>=3.4.5,<4
jsonschema>=4.10,<5
PyYAML>=6.0,<7
pytest>=8,<9
pytest-asyncio>=0.23,<1
# Declared (IA-07): calibration/pipeline tests read image dimensions via PIL.
Pillow>=10,<13
